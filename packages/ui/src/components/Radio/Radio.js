"use strict";
// import React from 'react';
// import { RadioProps } from './@types';
// import * as Styled from './Radio.styles';
// import RadioGroupContext, { RadioGroupContextType } from './Context';
// const Radio = React.forwardRef(({ value, label = value, style }: RadioProps, ref) => {
//   const radioGroup: RadioGroupContextType = React.useContext(RadioGroupContext);
//   const checked = value === radioGroup?.value;
//   const onChange = () => radioGroup?.onChange?.(value);
//   return (
//     <Styled.RadioWrapper ref={ref} style={style}>
//       <Styled.Input type="radio" checked={checked} onChange={onChange} />
//       <Styled.Check />
//       <Styled.Label>{label}</Styled.Label>
//     </Styled.RadioWrapper>
//   );
// });
// export default React.memo(Radio);
//# sourceMappingURL=Radio.js.map