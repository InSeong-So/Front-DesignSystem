import React from "react";
import { Header } from "unicorn-ui";

export const HeaderFromUILib = () => {
  return (
    <Header
      onLogin={() => {}}
      onCreateAccount={() => {}}
      onLogout={() => {}}
    >
    </Header>
  );
};
